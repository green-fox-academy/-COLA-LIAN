  'use strict';

// - Create a variable named `ak` and assign the value `123` to it
// - Create a function called `doubling` that doubles it's input parameter
// - Log the result of `doubling(ak)` to the console

var ak = 123;
function doubling(x) {
   var x2 = 2*x;
   return x2;   
}

console.info(doubling(ak));